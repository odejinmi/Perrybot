class Activities {
  String title, subtitle, avatar, amount, time;
  Activities(
      {required this.title,
      required this.subtitle,
      required this.avatar,
      required this.amount,
      required this.time});
}
